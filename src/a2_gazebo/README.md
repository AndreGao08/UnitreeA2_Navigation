# A2 Gazebo world

`a2_localization.sdf` targets Gazebo Fortress, the supported Gazebo Sim release
for ROS 2 Humble. It contains deliberately asymmetric geometry for evaluating
LiDAR-inertial odometry rather than an empty showcase world.
